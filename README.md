# MastersMRPEndToEndSelfDrivingRover
Project is an implementation of a full end-to-end self driving system where an AI agent is taught how to pilot a car using reinforcement learning. The training is done in a virtual environment using Carla and then the trained model is ported and integrated with an actual Rover. 
